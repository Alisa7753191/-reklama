# ПравоРеклама — проверка рекламы на правовые риски РФ

Веб-приложение (MVP) для проверки рекламных коммуникаций на соответствие
законодательству Российской Федерации: ФЗ-38 «О рекламе», КоАП РФ (ст. 14.3),
ФЗ-152 «О персональных данных», ФЗ-436 «О защите детей от информации»,
Закон «О защите прав потребителей».

По каждому найденному риску приложение показывает:

- **описание** нарушения и цитату из рекламы;
- **правовое основание** — закон и статью;
- **уровень риска** — низкий / средний / высокий / критический;
- **меры минимизации** — как исправить;
- **ответственность** — статью КоАП и диапазон штрафов (граждане / должностные / юрлица);
- **практику ФАС / судов** — при наличии в базе.

Готовый отчёт можно **выгрузить в PDF / распечатать** одной кнопкой
(печатная вёрстка с сохранением цветовой индикации рисков).

> ⚠️ **Дисклеймер.** Результаты формируются автоматически, носят
> информационно-справочный характер и **не являются юридической консультацией**.
> Правовая база и размеры штрафов могут устаревать — перед принятием решений
> проконсультируйтесь с юристом. База знаний (`backend/app/knowledge/*.yaml`)
> подлежит выверке специалистом.

## Как это работает (гибрид)

```
Ввод (текст / URL / изображение)
        │
        ▼
Нормализация в текст  (картинка → OCR/vision, URL → парсинг страницы)
        │
        ├─▶ Движок правил (детерминированно): стоп-слова, превосходная
        │   степень, отсутствие ERID/маркировки, категории товаров,
        │   обязательные дисклеймеры, сбор персданных
        │
        └─▶ LLM (Claude): смысловой анализ контекста и скрытых рисков
        │
        ▼
Слияние и дедуп находок → отчёт со «светофором» и карточками рисков
```

Движок правил работает **всегда**, в т.ч. без API-ключа. LLM-слой добавляет
смысловой анализ и включается при наличии `ANTHROPIC_API_KEY`.

## Поддерживаемые входы

- **Текст** — объявления, посты, слоганы.
- **URL / лендинг** — загрузка страницы и анализ её текста.
- **Изображение** — креативы (PNG/JPG/WEBP); текст распознаётся через
  vision-модель Claude, запасной вариант — Tesseract OCR.

## Автономная версия в один файл (без установки)

`standalone/index.html` — полностью самодостаточная версия проверки: движок
правил реализован на JavaScript и работает **прямо в браузере, офлайн, без
сервера**. Достаточно открыть файл двойным кликом. Подходит, чтобы быстро
попробовать проверку без запуска Docker.

Файл **генерируется** из той же правовой базы, что и бэкенд, — так они не
рассинхронизируются. После правок в `backend/app/knowledge/*.yaml` пересоберите:

```bash
python backend/tools/build_standalone.py   # standalone/template.html + YAML → standalone/index.html
```

> Ограничение: автономная версия выполняет только проверку **по правилам**
> (без ИИ-анализа смысла — он требует бэкенда с ключом). Полная функциональность —
> в основном приложении ниже.

## Быстрый старт (Docker)

```bash
cp .env.example .env      # при желании впишите ANTHROPIC_API_KEY
docker compose up --build
```

- Фронтенд: http://localhost:8080
- API (Swagger): http://localhost:8000/docs

## Запуск без Docker

### Бэкенд

```bash
cd backend
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
export ANTHROPIC_API_KEY=...     # опционально — включает ИИ-анализ
uvicorn app.main:app --reload --port 8000
```

Быстрый смоук-тест ядра (без сети и LLM):

```bash
python smoke_test.py
```

Автотесты (pytest, 27 тестов — движок правил, оркестратор, API):

```bash
pip install -r requirements-dev.txt
python -m pytest
```

### Фронтенд

```bash
cd frontend
npm install
npm run dev        # http://localhost:5173 (проксирует /api на :8000)
```

## Структура проекта

```
backend/
  app/
    api/          # роуты FastAPI (/api/analyze, /api/analyze/image, /api/health)
    ingest/       # приём контента: text, url, image (OCR/vision)
    rules/        # движок правил + морфология (pymorphy3)
    knowledge/    # ПРАВОВАЯ БАЗА (YAML): laws, liability, categories, rules, practice
    llm/          # адаптер провайдера (Claude / заглушка) + промпт
    analysis/     # оркестратор пайплайна
    models.py     # pydantic-модели (Finding, Report)
  smoke_test.py
frontend/
  src/            # React + TypeScript: ввод, отчёт, карточки рисков
docker-compose.yml
```

## Правовая база знаний

Вся правовая логика вынесена в редактируемые YAML-файлы
`backend/app/knowledge/` — их можно пополнять без изменения кода:

| Файл | Назначение |
|------|-----------|
| `laws.yaml` | Реестр законов и статей (ФЗ-38, 152-ФЗ, 436-ФЗ и др.) |
| `liability.yaml` | Ответственность (КоАП) и размеры штрафов |
| `categories.yaml` | Категории с особым режимом (алкоголь, табак, БАД, финансы…) |
| `rules.yaml` | Правила детекции (превосходная степень, маркировка, персданные…) |
| `practice.yaml` | Примеры практики ФАС / судов |

## Что покрывает

Правовая база построена по **действующей редакции ФЗ-38 «О рекламе» (ред.
27.10.2025)** и охватывает все существенные статьи (не только ст. 28):

- ✅ **Общие требования (ст. 5–9)**: превосходная степень, некорректное
  сравнение, гарантии результата, отсутствие существенной информации;
  дистанционная продажа (ст. 8 — реквизиты продавца); стимулирующие
  мероприятия/розыгрыши (ст. 9 — сроки и правила).
- ✅ **Маркировка**: ERID и пометка «реклама»/рекламодатель (ст. 18.1),
  реклама по сетям электросвязи и согласие (ст. 18), возрастная маркировка
  (436-ФЗ).
- ✅ **Финансы — раздельно по режимам ст. 28**:
  - **кредиты/займы/ипотека** — предупреждение «Оценивайте свои финансовые
    возможности и риски», «Изучите все условия кредита (займа)», ПСК (479-ФЗ);
  - **вклады** — раскрытие всех условий дохода (ст. 28 ч. 2 п. 2); фиксированная
    ставка допустима;
  - **инвестиции/форекс/ЦБ** — запрет гарантий доходности (ст. 28 ч. 2 п. 1),
    предупреждение о риске для форекс (ч. 5.1);
  - **ЦФА** (ст. 29.1), **ценные бумаги** (ст. 29).
- ✅ **Банкротство граждан (ст. 28.1, 332-ФЗ, с 01.01.2026)** — обязательное
  предупреждение и запрет обещаний списания долгов.
- ✅ **Ограниченные категории**: алкоголь (ст. 21), табак/вейпы (ст. 7 + ФЗ-15),
  лекарства и медуслуги (ст. 24), БАД (ст. 25), **энергетики** (ст. 25.1,
  393-ФЗ), **оружие** (ст. 26), азартные игры (ст. 27), **рента** (ст. 30).
- ✅ **Персональные данные** (152-ФЗ) и защита несовершеннолетних (ст. 6).

> Размеры штрафов и формулировки выверены по действующей редакции, но требуют
> периодической актуализации и проверки юристом.

## Дальнейшее развитие

- Смена LLM-провайдера на российский (GigaChat/YandexGPT) для хранения данных
  в РФ — заготовлен адаптер `app/llm/`.
- Рендеринг JS-лендингов через Playwright.
- Экспорт отчёта в PDF, история проверок, авторизация и тарифы.
- Анализ видео/аудио (в текущий MVP не входит).
